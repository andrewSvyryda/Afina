using DAL.Context;
using DAL.Entities;
using System.Linq;

namespace DAL.Queries
{
    /*class StudentQuery : IStudentQuery
    {
        public Student GetStudent(int id)
        {
            return AfinaContext.Context.Students.SingleOrDefault(s => s.Id == id);
        }

        public Student GetStudentByLogin(string login)
        {
            return AfinaContext.Context.Students.SingleOrDefault(s => s.Login == login);
        }

        public IQueryable<Student> GetStudents()
        {
            return AfinaContext.Context.Students;
        }
    }*/
}